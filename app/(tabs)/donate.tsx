// =============================================================================
// DONATE TAB - Placeholder (tab press intercepted → opens fullscreen /webview)
// =============================================================================
// This file must exist for Expo Router to register the tab, but navigation
// is intercepted in _layout.tsx listeners so this component never renders.

import { View } from 'react-native';

export default function DonateScreen() {
  return <View />;
}
